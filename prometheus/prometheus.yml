global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: seatrace-app
    metrics_path: /actuator/prometheus
    dns_sd_configs:
      - names:
          - app
        type: A
        port: 50000
